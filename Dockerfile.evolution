FROM node:18-alpine

WORKDIR /app

RUN npm install -g pm2

# Clone Evolution API do repositório oficial
RUN git clone https://github.com/EvolutionAPI/evolution-api.git . && \
    npm install

# Expor porta
EXPOSE 8080

# Comando para iniciar
CMD ["npm", "start"]
